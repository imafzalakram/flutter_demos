import 'package:flutter/material.dart';

void main() {
  runApp(
    Text(
      "Hello World!",
      textDirection: TextDirection.ltr,
      style: TextStyle(
        color: Colors.lightBlue,
        fontSize: 40.0,
      ),
    ),
  );
}

// Ctl+Shift+alt+L to reformat the code on Windows
// Cmd+Shift+Opt+L to reformate the code on Mac
